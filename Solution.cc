#include <bits/stdc++.h>
using namespace std;

void solve() {
	cout << "Hello World!" << endl;
}

int main() {
	#ifdef OFFLINE
	// freopen("input.txt", "r", stdin);
	// freopen("output.txt", "w", stdout);
	#endif

	solve();
	
	#ifdef OFFLINE
	system("pause");
	#endif
	return 0;
}